name: CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  NODE_VERSION: '20'

jobs:
  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        # TODO: Remove continue-on-error once existing lint errors are fixed (242 errors)
        continue-on-error: true
        run: npm run lint

      - name: Run TypeScript check
        run: npm run type-check

  build:
    name: Build Application
    runs-on: ubuntu-latest
    needs: lint
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Next.js app
        run: npm run build
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: .next/
          retention-days: 7

  # UNIT TESTS - Descomentar cuando estén listos
  # test-unit:
  #   name: Unit Tests
  #   runs-on: ubuntu-latest
  #   needs: build
  #   timeout-minutes: 10
  #
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: 'npm'
  #     - run: npm ci
  #     - run: npm run test -- --coverage
  #     - name: Upload coverage
  #       uses: codecov/codecov-action@v4
  #       with:
  #         files: ./coverage/coverage-final.json

  # E2E TESTS - Descomentar cuando estén listos
  # test-e2e:
  #   name: E2E Tests
  #   runs-on: ubuntu-latest
  #   needs: build
  #   timeout-minutes: 20
  #
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: 'npm'
  #     - run: npm ci
  #     - run: npx playwright install --with-deps
  #     - run: npm run test:e2e
  #       env:
  #         TEST_USER_EMAIL: ${{ secrets.TEST_USER_EMAIL }}
  #         TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
  #     - uses: actions/upload-artifact@v4
  #       if: always()
  #       with:
  #         name: playwright-report
  #         path: playwright-report/

  success:
    name: CI Success
    runs-on: ubuntu-latest
    needs: [lint, build] # Agregar test-unit y test-e2e cuando estén listos
    if: success()

    steps:
      - name: CI Passed
        run: echo "CI Pipeline completed successfully"
