export interface ResponseTemplate {
    id: string;
    agency_id: string;
    name: string;
    content: string;
    channel: string;
    category: string;
    usage_count: number;
    is_active: boolean;
    created_at: string;
}
