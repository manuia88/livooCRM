import InboxLayout from '@/components/inbox/InboxLayout';

export const metadata = {
    title: 'Inbox | CRM',
    description: 'Gestión de comunicaciones multicanal',
};

export default function InboxPage() {
    return <InboxLayout />;
}
